# 项目信息

> 本文件由主编排器（`/ivd-00-orchestrator`）Phase 0 自动生成，也可手动填写。
> 带 `★` 的字段必须由人工确认（无法从 IFU/法规自动推断）；其余字段由预处理 skill 自动提取填充。

## 项目基本信息

| 字段 | 值 | 来源 |
|------|-----|------|
| full_name（产品全称） |  | 00b 从考核 IFU 提取 |
| protocol_number（方案编号）★ |  | 人工确认 |
| version_date（方案版本日期）★ |  | 人工确认 |
| product_type（定性/定量）★ |  | 00b 从 IFU 推断，人工确认 |
| management_category（第二类/第三类） |  | 00a 从指导原则推断 |

## 被测物信息

| 字段 | 值 |
|------|-----|
| name（被测物名称） |  |
| english_name（英文名/缩写） |  |
| sample_types（样本类型，如 ["血清","血浆"]） |  |

## 适应证信息

| 字段 | 值 |
|------|-----|
| name（适应证） |  |
| target_population（适用人群） |  |
| existing_methods（现有诊断方法） |  |

> 生物学背景、流行病学背景、诊断标准等长文本由 `专家共识提取.md` 提供，此处不重复。

## 性能参数

### 定性产品用

| 字段 | 值 | 说明 |
|------|-----|------|
| pos_agreement_target（阳性符合率目标值 P₀）★ |  | 如 0.90 |
| pos_agreement_expected（阳性符合率预期值 P_T）★ |  | 如 0.95 |
| neg_agreement_target（阴性符合率目标值 P₀）★ |  | 如 0.90 |
| neg_agreement_expected（阴性符合率预期值 P_T）★ |  | 如 0.97 |

### 定量产品用

| 字段 | 值 | 说明 |
|------|-----|------|
| expected_correlation（ρ₁）★ |  | 如 0.985 |
| min_correlation（ρ₀） |  | EP09-A3 默认 0.975 |
| bias_mean（μ %）★ |  | 如 0 |
| bias_sd（σ %）★ |  | 如 12 |
| acceptable_bias_pct（δ %）★ |  | 如 30 |
| loa_limit（临床可接受偏倚限值 %）★ |  | 如 60 |

### 通用

| 字段 | 值 |
|------|-----|
| dropout_rate（脱落剔除率） | 0.05 |

## 统计参数（通常默认值即可）

| 字段 | 值 |
|------|-----|
| alpha（检验水准） | 0.05 |
| beta（Ⅱ类错误） | 0.20 |
| power（检验效能） | 0.80 |

## EQA 标准

| 字段 | 值 |
|------|-----|
| standard_name（室间质评标准名称） |  |
| acceptable_range_pct（可接受范围 %） |  |

## 项目特有参考文献

（如有需补充进第十九章的项目特有文献，逐条列出，否则留空）

-
